'use client';
// Update the import path if the alias '@' is not configured, or fix the path as needed
import ReportingChatbot from '../components/ReportingChatbot';

export default function Page() {
  return (
    <main className="min-h-screen">
      <ReportingChatbot />
    </main>
  );
}